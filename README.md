# SprintBoard

Project structure:

```text
sprintboard/
  sprintboard_BE/
  sprintboard-fe/
```

Backend:

```bash
cd sprintboard_BE
npm install
npm run dev
```

Frontend is built inside `sprintboard-fe`.
