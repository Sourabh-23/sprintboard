# SprintBoard

Project structure:

```text
sprintboard/
  sprintboard_BE/
  sprintboard_FE/
```

Backend:

```bash
cd sprintboard_BE
npm install
npm run dev
```

Frontend will be built inside `sprintboard_FE`.
